services:
  MT5Linux_RakiFella:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: lprett/MT5Linux_RakiFella:local
    container_name: MT5Linux_RakiFella
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "18812:18812"
    environment:
      - LOGIN=${MT5_LOGIN:-}
      - PASSWORD=${MT5_PASSWORD:-}
      - SERVER=${MT5_SERVER:-}
      - MT5_HOST=0.0.0.0
      - RPYC_PORT=18812
